services:
  file-agent:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - "./data:/app/data"
    env_file:
      - .env
    environment:
      - DB_PATH=/app/data/file_index.db
      - FILE_AGENT_PORT=8080
